import React, { Fragment } from 'react';
import ChatPanel from '../../components/ChatPanel/ChatPanel';

const Chat = () => {
  return (
    <Fragment>
      <ChatPanel />
    </Fragment>
  );
};

export default Chat;