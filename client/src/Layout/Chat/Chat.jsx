import React, { Fragment } from 'react';
import ChatPanel from '../../components/Chat/Chat';

const Chat = () => {
  return (
    <Fragment>
      <ChatPanel />
    </Fragment>
  );
};

export default Chat;